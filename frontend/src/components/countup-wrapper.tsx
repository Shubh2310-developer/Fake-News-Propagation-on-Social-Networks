import CountUp from "react-countup";
import { CountUpProps } from "react-countup";

const CountUpWrapper = (props: CountUpProps) => {
  return <CountUp {...props} />;
};

export default CountUpWrapper;
